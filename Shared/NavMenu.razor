<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">Firma de Abogados</a>
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>

<div class="@NavMenuCssClass nav-scrollable" @onclick="ToggleNavMenu">
    <nav class="flex-column">

        <div class="nav-item px-3">
            <NavLink class="nav-link" id="casos_registrados" href="imprimir" Match="NavLinkMatch.All">
                <span class="oi oi-document" aria-hidden="true"></span> Casos
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" id="clientes_registrados_ir" href="imprimirCliente" Match="NavLinkMatch.All">
                <span class="oi oi-document" aria-hidden="true"></span> Clientes
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" id="abogados_registrados" href="imprimirAbogado" Match="NavLinkMatch.All">
                <span class="oi oi-document" aria-hidden="true"></span> Abogados
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" id="salir" href="" Match="NavLinkMatch.All">
                <span class="oi oi-account-logout" aria-hidden="true"></span> Salir
            </NavLink>
        </div>

    </nav>
</div>

@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
